# S3/DynamoDB backend (no secrets here)
BUCKET=your-s3-bucket-name
DDB=your-dynamodb-table-name
REGION=eu-west-3

# Prefix used for S3 state paths (per workspace)
# Final path: s3://$BUCKET/$KEY_PREFIX/$WORKSPACE/terraform.tfstate
KEY_PREFIX=project/states

